extends:
  - recommended

apis:
  bigg:
    root: ./specs/bigg/openapi.yaml
    rules:
      no-ambiguous-paths: error  

theme:
  openapi:
    schemaExpansionLevel: 2
    requiredPropsFirst: true
    theme:
      sidebar:
        backgroundColor: '#ffffff'
    